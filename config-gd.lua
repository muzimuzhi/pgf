-- Tests for graph drawing (gd) library

stdengine      = "luatex"
checkengines   = {"luatex"}
testfiledir    = maindir .. "/testfiles-gd"

-- extra support file(s)
supportdir     = maindir .. "/testfiles/support"
checksuppfiles = {"pgf-regression-test.tex"}
